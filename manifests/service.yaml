apiVersion: v1
kind: Service
metadata:
  name: testworkflow
  namespace: aksstore
spec:
  type: LoadBalancer
  selector:
    app: testworkflow
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000